doctype html
html(lang="es")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Burrowed - Login
    link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css")
    link(rel="stylesheet" href="/bootstrap/css/styles.css")
  body


    block content
      .login-container
        .login-carousel
          // Carrusel usando Bootstrap
          #carouselInfo.carousel.slide(data-bs-ride="carousel")
            .carousel-inner
              .carousel-item.active
                img(src="/bootstrap/img/fachada.png" class="d-block w-100 rounded" alt="Laboratorio 1")
                .carousel-caption.d-none.d-md-block
                  h5 Innovación y tecnología
                  p Descubre los laboratorios de ESCOM y sus recursos.
              .carousel-item
                img(src="/bootstrap/img/laboratorio_electronica.png" class="d-block w-100 rounded" alt="Laboratorio 2")
                .carousel-caption.d-none.d-md-block
                  h5 Seguridad y control
                  p Gestiona tus préstamos y reservaciones de manera segura.
              .carousel-item
                img(src="/bootstrap/img/responsive.png" class="d-block w-100 rounded" alt="Laboratorio 3")
                .carousel-caption.d-none.d-md-block
                  h5 Acceso fácil
                  p Solicita materiales y espacios desde cualquier dispositivo.
            button.carousel-control-prev(type="button" data-bs-target="#carouselInfo" data-bs-slide="prev")
              span.carousel-control-prev-icon(aria-hidden="true")
              span.visually-hidden Anterior
            button.carousel-control-next(type="button" data-bs-target="#carouselInfo" data-bs-slide="next")
              span.carousel-control-next-icon(aria-hidden="true")
              span.visually-hidden Siguiente
        .login-form-section
          .brand-logos
            img(src="/bootstrap/img/burrowed_logo.png" alt="Burrowed Logo")
            //img(src="/bootstrap/img/escom_logo.png" alt="ESCOM Logo")
          .burrowed-title Burrowed
          .burrowed-slogan Tu laboratorio, en la palma de tu mano.
          if error
            p(style="color: #d32f2f; text-align:center; margin-bottom:1rem;") #{error}
          form.login-form(action="/login" method="POST")
            .mb-3
              label(for="email") Correo institucional
              input#email(type="email" name="email" required placeholder="usuario@alumno.ipn.mx")
            .mb-3
              label(for="password") Contraseña
              input#password(type="password" name="password" required placeholder="Contraseña")
            button(type="submit") Iniciar sesión
          a(href="/registro" class="btn btn-link mt-2") Crear cuenta
      script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js")
