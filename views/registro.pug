extends layout

block content
  head
    link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css")
    link(rel="stylesheet" href="/bootstrap/css/styles.css")
  .register-bg
    .register-container
      .text-center.mb-4
        img(src="/bootstrap/img/burrowed_logo.png" alt="Burrowed Logo" style="height:110px;max-width:200px;")
        h2.burrowed-title.mt-3 Crear cuenta
        p.burrowed-slogan.text-center Crea tu acceso a Burrowed y gestiona tus laboratorios fácilmente.
      form(method="POST" action="/registro" enctype="multipart/form-data" class="register-form mx-auto")
        .mb-3
          label.form-label(for="nombre") Nombre
          input.form-control(type="text" name="nombre" id="nombre" required)
        .mb-3
          label.form-label(for="apellido_paterno") Apellido paterno
          input.form-control(type="text" name="apellido_paterno" id="apellido_paterno" required)
        .mb-3
          label.form-label(for="apellido_materno") Apellido materno
          input.form-control(type="text" name="apellido_materno" id="apellido_materno" required)
        .mb-3
          label.form-label(for="correo") Correo institucional
          input.form-control(type="email" name="correo" id="correo" required)
        .mb-3
          label.form-label(for="carrera") Carrera
          select.form-control(name="carrera" id="carrera" required)
            option(value="") Selecciona una carrera
            option(value="ISC") ISC
            option(value="IIA") IIA
            option(value="LCD") LCD
        .mb-3
          label.form-label(for="password") Contraseña
          input.form-control(type="password" name="password" id="password" required)
        .mb-3
          label.form-label(for="credencial") Adjunta tu credencial escolar
          input.form-control(type="file" name="credencial" id="credencial" accept="image/*,application/pdf" required)
        button.btn.btn-primary.w-100.mt-2(type="submit") Registrarse
      .text-center.mt-3
        a(href="/login" class="btn btn-link") ¿Ya tienes cuenta? Inicia sesión
